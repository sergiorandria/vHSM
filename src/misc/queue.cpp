#include "queue.h"

// Implementation file for queue. Functions are defined in queue.h (header-only) or
// will be linked from other translation units. This file exists to allow building
// projects that expect a compilation unit for the queue module.
