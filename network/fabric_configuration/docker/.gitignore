# Generated by generate-network.sh / enroll-network.sh — never commit.
# (Mirrors the pre-relocation behaviour: only scripts + templates are tracked.)
network.env
.env
configtx.yaml
channel-artifacts/
organizations/
peer-data/
fabric-ca/
builders/
